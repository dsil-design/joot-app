name: "💾 Test Database Backup"
description: "Test local database backup functionality"
command: "export $(grep -v '^#' .env.deployment | xargs) && ./scripts/backup-db.sh {{backup_name}}"
tags:
  - backup
  - database
  - testing
arguments:
  - name: backup_name
    description: "Name for the backup (optional)"
    default_value: "warp_test_backup"
