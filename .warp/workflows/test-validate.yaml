name: "🧪 Test & Validate"
description: "Validate AI-generated code without deployment"
command: "gh workflow run warp-test-validate.yml --repo dsil-design/joot-app --field run_all_tests={{run_all_tests}} --field coverage_required={{coverage_required}}"
tags:
  - testing
  - validation
  - pre-deployment
  - ai-code
arguments:
  - name: run_all_tests
    description: "Run comprehensive test suite (true/false)"
    default_value: "false"
  - name: coverage_required
    description: "Require minimum test coverage (true/false)"
    default_value: "true"
