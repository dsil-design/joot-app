name: "🗄️ Deploy Database Changes"
description: "Trigger deployment with database migrations only"
command: "gh workflow run cicd.yml --repo dsil-design/joot-app --field environment={{environment}}"
tags:
  - database
  - migration
  - deployment
arguments:
  - name: environment
    description: "Target environment"
    default_value: "production"
